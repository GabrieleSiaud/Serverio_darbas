version: "2"
sql:
  - engine: "postgresql"
    queries: "./queries"
    schema:
      - "./migrations"
    gen:
      go:
        package: "repository"                       # paketo pavadinimas, kurį importuos main.go
        out: "internal/generated/repository"       # kur sugeneruoti Go failus
        sql_package: "pgx/v5"
